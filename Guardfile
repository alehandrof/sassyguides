# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'compass' do
  # watch('^src/(.*)\.s[ac]ss')
  # watch('.+\.s[ac]ss')
  # watch(%r{.+\.s[ac]ss$})
  watch(%r{sass/.+\.s[ac]ss$})
end

guard 'livereload' do
  # watch(%r{.+\.(css|js|html?)$})
  watch(%r{.+\.(css|js|html?)$})
end
